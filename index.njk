<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Document</title>
        <style>
            * {
                box-sizing: border-box;
            }
            html {
                font-size: 140px;
                height: 100%;
            }
            body,
            html {
                width: 100%;
                margin: 0;
                padding: 0;
            }
            .container {
                width: 100vw;
                display: flex;
                flex-direction: row;
                flex-wrap: wrap;
                justify-content: space-evenly;
                align-items: center;
                margin-top: 130px;
                margin-bottom: 100px;
            }
            img {
                display: block;
                vertical-align: bottom;
                font-size: 100%;
                position: relative;
            }
            header {
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 120px;
            }
            header img {
                vertical-align: top;
                width: 100%;
                height: 100%;
            }
            footer {
                margin-top: -40px;
                width: 100%;
                height: 70px;
            }
            footer img {
                width: 100%;
                height: 100%;
            }
            .img_item {
                margin-top: 70px;
                position: relative;
                width: 1rem;
                height: 1.78rem;
                margin: 100px calc((100vw - 2rem) / 6) 0 calc((100vw - 2rem) / 6);
            }
            .img_item.horizontal {
                margin-top: 70px;
                width: 1.78rem;
                height: 1rem;
            }
            .img_item.horizontal ~ .img_item.horizontal {
                margin-top: 40px !important;
            }
            .img_front {
                position: absolute;
                height: 100%;
                width: 100%;
                z-index: 100;
            }
            .img_bg {
                width: 114%;
                height: 132.5%;
                left: -0.07rem;
                top: -0.28rem;
                position: absolute;
            }
            .img_item.horizontal .img_bg {
                width: 63.3%;
                height: 233%;
                left: 0.313rem;
                top: -0.664rem;
                position: absolute;
                transform: rotateZ(90deg);
            }
            h4 {
                text-align: center;
                position: relative;
                top: 10px;
                font-size: 16px;
                color: #122d3f;
            }
        </style>
    </head>
    <body>
        <header>
            <img src="./toutu.png" alt="">
        </header>
        {# <h4>{{ title }}</h4> #}
        <div class="container">
                {% for item in filelist %}
                    {% if item.orientation === 'vertical' %}
                        <div class="img_item">
                            <img class="img_bg" src="手机.png" alt="">
                            <img class="img_front" src="{{ item.uri }}" alt="">
                        </div>
                    {% else %}
                        <div class="img_item horizontal">
                            <img class="img_bg" src="手机.png" alt="">
                            <img class="img_front" src="{{ item.uri }}" alt="">
                        </div>
                    {% endif %}
                {% endfor %}
        </div>
        <footer>
            <img src="./weitu.png" alt="">
        </footer>
    </body>
</html>